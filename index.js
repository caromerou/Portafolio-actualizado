import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);

//Este es el archivo  encargado de llamar a la App, y desde acá es que el html, llama y renderiza la aplicacion web, o 
//como en este caso el portafolio.
